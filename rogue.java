public class rogue extends Character {
    
    boolean isInvisible;

	public rogue(String r) {
		health = 100;
		damage = 15;
		race = r;
	}
	
	void attack (Character target){
        
        if (target instanceof engineer) {
			target.takeDamage 
 }

        if (target instanceof mage) {
			target.takeDamage 
        }

        if (target instanceof barbarian) {
			target.takeDamage 
        }

}


void trackerDeployed() {
    deployedDevice = true;
    builtDevice = "TRACKER";
    
  
    if (rog.isInvisible) {
        rog.isInvisible = false;
    
    }
}

//DO IF BARBARIAN RAGES 

void takeDamage(int damage) {
    if 
	
	else (this.isInvisible) {
        return;
    }
    this.health -= damage;
}


void activateInvisibilitiy() {
	isInvisable = true 
	this.health -= damage;
}

void deactivateInvisibility() {
	isInvisable = false 
	this.health += damage;
}


